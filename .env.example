# Copy to .env and set your values
# Database (MySQL)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=u_diep

# JWT (use a long random string in production)
JWT_SECRET=justice00711
JWT_EXPIRES_IN=7d

# Server
PORT=3000

# Announcement (shown when user opens the game; leave empty to show nothing)
# ANNOUNCEMENT_TITLE=Welcome
# ANNOUNCEMENT_CONTENT=Server maintenance on Sunday 10:00–12:00 UTC.
